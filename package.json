{
  "name": "appreciator",
  "version": "0.0.1",
  "description": "This app is a small CLI app (and possibly web) that will let you input data about gifts, givers and personalised messages. It will then generate emails from a template then send the emails to the relevant people. ``` Available Commands:   add         Adds an entry   clear       Removes all entries   list        List all entries   gen         Generates emails of appreciation and prints to STDOUT   send        Generates then sends email using SMTP defined in the config   web         Begin a web server to allow input from an internet browser ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nii236/appreciator.git"
  },
  "author": "",
  "license": "WTFPL",
  "bugs": {
    "url": "https://github.com/nii236/appreciator/issues"
  },
  "homepage": "https://github.com/nii236/appreciator#readme",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-livereload": "^3.8.1",
    "gulp-sync": "^0.1.4",
    "gulp-util": "^3.0.7"
  }
}
